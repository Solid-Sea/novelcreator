ollama:
  endpoint: "http://localhost:11434/api/generate"
  model: "deepseek-r1:32b"  # 或您安装的其他模型
  temperature: 0.6  # 增加创意性

paths:
  novels_dir: "./novels"
  
settings:
  min_chapter_words: 4000
  max_retries: 10      # 增加重试次数
  timeout: 3000        # 超时时间(秒)
